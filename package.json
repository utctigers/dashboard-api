{
  "name": "dashboard-api",
  "version": "1.0.0",
  "description": "Backend API for DynamoDB Business Dashboard",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "setup:dynamodb": "node setup-dynamodb-tables.js",
    "test:api": "cd e2e && npx playwright test",
    "test:api:headed": "cd e2e && npx playwright test --headed",
    "test:api:ui": "cd e2e && npx playwright test --ui"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.913.0",
    "@aws-sdk/lib-dynamodb": "^3.913.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "nodemon": "^3.0.1"
  }
}
